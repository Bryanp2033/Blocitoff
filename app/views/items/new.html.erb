<h1>Create items for your list</h1>
<div class="row">
	<div class="col-md-8">
	
		<%= form_for [current_user, @item] do |f| %>
	<%= f.text_field :name , class: 'form-control', placeholder: "Enter item" %>
	<%= f.submit "Save", class: 'btn btn-success' %>
	<% end %>

			
		
		
	</div>
</div>
